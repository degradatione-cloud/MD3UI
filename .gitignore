# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
gradle-app.setting
.gradletasknamecache

# Loom: decompiled sources and remapped jars, all regenerable and large
.fabric/
run/
remappedSrc/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.settings/
.classpath
.project
out/
bin/

# OS
.DS_Store
Thumbs.db

# Local test scaffolding
libs/junit-standalone.jar
dist/
/tmp/

# Generated artefacts are committed on purpose: the screenshots in README must
# match the code that produced them, and design/tokens.json is consumed by
# designers who should not need a JDK to get it. CI regenerates and uploads both,
# so a stale commit is visible in the diff.

# scratch files from font baking / dependency research
font.json
deps.json
